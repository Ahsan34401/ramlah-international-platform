DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-with-long-random-string-min-32-chars"
ADMIN_SEED_EMAIL="admin@ramlah.local"
ADMIN_SEED_PASSWORD="changeme123"
ADMIN_SEED_NAME="Ramlah Admin"

# Public URL for links in applicant emails (no trailing slash)
PUBLIC_SITE_URL="http://localhost:3000"

# Transactional email (optional — leave SMTP_HOST empty to disable)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_SECURE="false"
SMTP_FROM="Ramlah International <noreply@example.com>"

# Fallback team inbox if notify_email is not set in Site settings
NOTIFY_EMAIL=""
